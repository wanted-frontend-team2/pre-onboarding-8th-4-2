import Comments from "../components/Comments";

const Home = () => {
  return (
    <main className="fixed flex justify-center items-center w-full h-full bg-slate-800 ">
      <Comments />
    </main>
  );
};

export default Home;
